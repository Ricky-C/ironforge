{
  "name": "@ironforge/cost-reporter",
  "version": "0.0.0",
  "private": true,
  "description": "Daily cost reporter Lambda — queries Cost Explorer and publishes to the cost-alerts SNS topic.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "node ./build.mjs",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.10.0",
    "@aws-sdk/client-cost-explorer": "^3.700.0",
    "@aws-sdk/client-sns": "^3.700.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
