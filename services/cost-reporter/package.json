{
  "name": "@ironforge/cost-reporter",
  "version": "0.0.0",
  "private": true,
  "description": "Daily cost reporter Lambda — queries Cost Explorer and publishes to the cost-alerts SNS topic.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "node ./build.mjs",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.10.0",
    "@aws-sdk/client-cost-explorer": "^3.1039.0",
    "@aws-sdk/client-sns": "^3.1039.0",
    "zod": "^4.4.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  }
}
