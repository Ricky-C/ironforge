{
  "name": "@ironforge/api",
  "version": "0.0.0",
  "private": true,
  "description": "Ironforge portal API — Hono on Lambda. Initial scope: Cognito JWT auth middleware.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "node ./build.mjs",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.10.0",
    "aws-jwt-verify": "^5.1.1",
    "hono": "^4.12.16",
    "zod": "^4.4.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "vite": "^7.3.2",
    "vitest": "^4.1.5"
  }
}
