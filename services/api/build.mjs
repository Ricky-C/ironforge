// Lambda entry point lands in PR #2 (API Gateway + Hono handler skeleton).
// This stub keeps `pnpm -r run build` green while only the auth middleware exists.
console.log("@ironforge/api: no Lambda entry yet — build is a no-op");
