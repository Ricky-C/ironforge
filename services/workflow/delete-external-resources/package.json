{
  "name": "@ironforge/workflow-delete-external-resources",
  "version": "0.0.0",
  "private": true,
  "description": "Workflow task Lambda — Phase 1.5 deprovision SFN State 2 happy path. Deletes the GitHub repo + tfstate file via @ironforge/destroy-chain, then transitions Service deprovisioning → archived and Job running → succeeded. Throws on any sub-op failure so SFN's Catch routes to DeprovisionFailed.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "node ./build.mjs",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ironforge/destroy-chain": "workspace:*",
    "@ironforge/shared-types": "workspace:*",
    "@ironforge/shared-utils": "workspace:*",
    "zod": "^4.4.1"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.1042.0",
    "@aws-sdk/lib-dynamodb": "^3.1042.0",
    "@types/node": "^25.6.0",
    "aws-sdk-client-mock": "^4.1.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "vite": "^7.3.2",
    "vitest": "^4.1.5"
  }
}
