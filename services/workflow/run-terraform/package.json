{
  "name": "@ironforge/workflow-run-terraform",
  "version": "0.0.0",
  "private": true,
  "description": "Workflow task Lambda — run-terraform. Executes terraform init/apply against the per-template terraform module bundled in the container image (per ADR-009: Lambda direct execution + container image deploy). Per-service backend state lives at s3://ironforge-tfstate-<env>-<account>/services/<service-id>/.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "node ./build.mjs",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ironforge/shared-types": "workspace:*",
    "@ironforge/shared-utils": "workspace:*"
  },
  "devDependencies": {
    "@aws-sdk/lib-dynamodb": "^3.1039.0",
    "@types/node": "^25.6.0",
    "aws-sdk-client-mock": "^4.1.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  }
}
