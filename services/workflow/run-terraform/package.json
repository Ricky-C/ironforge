{
  "name": "@ironforge/workflow-run-terraform",
  "version": "0.0.0",
  "private": true,
  "description": "Workflow task Lambda — run-terraform. PR-C.2 stub: thin facade over @ironforge/workflow-stub-lib's stubTask. Replaced at PR-C.6 with the real CodeBuild-orchestrated terraform apply (per pre-PR ADR).",
  "license": "UNLICENSED",
  "scripts": {
    "build": "node ./build.mjs",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ironforge/shared-types": "workspace:*",
    "@ironforge/workflow-stub-lib": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  }
}
