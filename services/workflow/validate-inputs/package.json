{
  "name": "@ironforge/workflow-validate-inputs",
  "version": "0.0.0",
  "private": true,
  "description": "Workflow task Lambda — validate-inputs. Validates the user's wizard inputs against the per-template inputs schema declared in TEMPLATE_REGISTRY (@ironforge/shared-types).",
  "license": "UNLICENSED",
  "scripts": {
    "build": "node ./build.mjs",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ironforge/shared-types": "workspace:*",
    "@ironforge/shared-utils": "workspace:*",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@aws-sdk/lib-dynamodb": "^3.1039.0",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^25.6.0",
    "aws-sdk-client-mock": "^4.1.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  }
}
