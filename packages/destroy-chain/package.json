{
  "name": "@ironforge/destroy-chain",
  "version": "0.0.0",
  "private": true,
  "description": "Reusable destroy chain primitives — terraform destroy, GitHub repo delete, tfstate delete. Returns structured Outcomes; caller decides log/continue/throw flow control. Consumed by cleanup-on-failure (best-effort recovery) and (Phase 1.5) the deprovision delete-external-resources Lambda (DELETE flow, throws on partial failure).",
  "license": "UNLICENSED",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "node ./build.mjs",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.1039.0",
    "@aws-sdk/client-s3": "^3.1039.0",
    "@ironforge/shared-utils": "workspace:*"
  },
  "devDependencies": {
    "@smithy/util-stream": "^4.5.25",
    "@types/node": "^25.6.0",
    "aws-sdk-client-mock": "^4.1.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  }
}
