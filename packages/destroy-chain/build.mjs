// @ironforge/destroy-chain is a TypeScript-only library consumed via
// the "exports" field pointing at src/. No bundling step needed; consumer
// bundlers (esbuild for Lambdas) handle TS directly. This stub keeps
// `pnpm -r run build` green.
console.log("@ironforge/destroy-chain: source-only package — build is a no-op");
